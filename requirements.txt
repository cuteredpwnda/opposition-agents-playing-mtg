# Core runtime requirements (mirrors `pyproject.toml [project] dependencies`).
# For ML / ontology / UI features use the optional extras instead:
#     pip install -e .[ml]
#     pip install -e .[ontology]
#     pip install -e .[ui]
#     pip install -e .[dev]
#     pip install -e .[all]
#
# This file is kept for environments that prefer plain `pip install -r`.
-e .